<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quote of the Day</title>
 

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body style="background-color: rgb(156, 156, 156);">
    <div class="container mt-5 text-center">
        <div class="card shadow-lg p-4 mx-auto" style="max-width: 600px;">
            <h2 class="mb-3 text-primary">Quote of the Day</h2>
            <blockquote class="blockquote">
                <p class="quote mb-0">"<%= quote.body %>"</p> <br>
                <footer class="blockquote-footer author"> <%= quote.author %></footer>
            </blockquote>

            <div class="d-flex justify-content-center gap-2 mt-2">
                <form action="/" method="get">
                    <button type="submit" class="btn btn-outline-info">Get New Quote</button>
                </form>
                <form action="/save" method="post">
                    <input type="hidden" name="text" value="<%= quote.body %>">
                    <input type="hidden" name="author" value="<%= quote.author %>">
                    <button type="submit" class="btn btn-outline-success">Save Quote</button>
                </form>
            </div>

            <hr>


            <form action="/search" method="get" class="mt-3">
                <div class="input-group">
                    <input type="text" name="q" class="form-control" placeholder="Search by Author" required>
                    <button type="submit" class="btn btn-dark">Search</button>
                </div>
            </form>
        </div>

        <h3 class="mt-5 text-primary">Saved Quotes</h3>
        <div class="mx-auto" style="max-width: 600px;">
            <% if (savedQuotes.length > 0) { %>
                <div class="row row-cols-1 g-3">
                    <% savedQuotes.forEach(quote => { %>
                        <div class="col">
                            <div class="card shadow-sm shadow-lg p-4 mx-auto">
                                <div class="card-body">
                                    <blockquote class="blockquote mb-0">
                                        <p class="mb-2 text-secondary">"<%= quote.text %>"</p><br>
                                        <footer class="blockquote-footer">
                                            <strong class="text-dark"><%= quote.author %></strong>
                                        </footer>
                              
                                    </blockquote>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <p class="text-muted text-center mt-3">No saved quotes yet.</p>
            <% } %>
        </div>
        <br><br>
    </div>
</body>
</html>
